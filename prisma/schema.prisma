datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Categorias {
  categoria_id Int    @id @default(autoincrement())
  carta_id     Int
  nombre       String
  orden        Int    @default(0)
  
  @@map("categorias")
}